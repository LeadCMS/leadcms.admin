import { styled } from "@mui/material";
import { LocalContainerProps } from "types";
import { Title } from "./index";

const TitleContainer = styled(Title)<LocalContainerProps>``;

export { TitleContainer };
