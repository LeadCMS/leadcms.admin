import { BreadcrumbLink } from "types";
import { dataListBreadcrumbLinks } from "utils/constants";

export const settingsFormBreadcrumbLinks: BreadcrumbLink[] = [...dataListBreadcrumbLinks];

export const settingsCurrentBreadcrumb = "System Settings";
